{
  "name": "frostfire-gateway",
  "version": "1.0.0",
  "description": "Load balancer gateway for Frostfire Forge game servers",
  "module": "gateway-server.ts",
  "scripts": {
    "production": "bun --env-file=.env.production start.ts",
    "development": "bun --env-file=.env.development start.ts",
    "create-config": "bun --env-file=.env.production ./utility/create-config.ts"
  },
  "keywords": [
    "gateway",
    "load-balancer",
    "game-server"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/bun": "^1.3.8",
    "@types/node": "^25.2.2"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "type": "module",
  "private": true,
  "dependencies": {
    "@types/nodemailer": "^7.0.9",
    "nodemailer": "^8.0.1"
  }
}
